
This repo is used for all assignments of the SWP course. 
To add the framework for the practical assignment run


`git subtree add --prefix=solP git@git-students.sai.tugraz.at:swp25/ap-framework.git main`
